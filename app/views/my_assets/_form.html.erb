<%= form_for([@project, @my_asset]) do |f| %>
  <% if @my_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_asset.errors.count, "error") %> prohibited this my_asset from being saved:</h2>

      <ul>
      <% @my_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
